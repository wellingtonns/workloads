apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-dev
    external-dns.alpha.kubernetes.io/hostname: react-app.dominio.com.br
spec:
  rules:
    - host: react-app.dominio.com.br
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: react-app
                port:
                  number: 80
  tls:
    - hosts:
        - react-app.dominio.com.br
      secretName: react-app-letsencrypt-secret